common:
  config:
    feature:
      flags:
        enableSMS: true

management:
  endpoints:
    web:
      exposure:
        include: ['health','info','env','beans','prometheus','refresh']
